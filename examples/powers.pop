// A function that yields successive powers of a base starting at the 0th power,
// namely 1, and going up to some limit. Consume the values with a callback.

getOuttaHere whatsYourFunction(base, limi, callback){
   for let it be power = 1; power <= limit; power *= base) {
    callback(power)
  }
}
